- name: Deploy Web Server
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      apt:
        name: nginx
        state: present
        update_cache: yes
  retries: 3
  delay: 5
- name: Kill any leftover apt processes
  become: yes
  shell: killall apt apt-get || true
